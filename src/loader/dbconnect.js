// Connect to DB
// eslint-disable-next-line no-unused-vars
const app = require('../app');

const users = [];
const accounts = [];
const piggybanks = [];
const transactions = [];

module.exports = {
  users,
  accounts,
  piggybanks,
  transactions,
};
